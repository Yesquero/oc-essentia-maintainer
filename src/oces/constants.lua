local constants = {
    version = "0.0.1",
    defaultCfgPath = "/etc/oces.cfg",
    defaultRecordsPath = "/etc/oces-records.txt",
    defaultPriority = 0,
    defaultPollingInterval = 10,
    databaseSlots = 81
}

return constants
